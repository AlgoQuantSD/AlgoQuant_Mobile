import React from "react";
import { View, StyleSheet, SafeAreaView, Text } from "react-native";

export default function BacktestingScreen() {
  return (
    <SafeAreaView>
      <Text>Backtesting screen</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
